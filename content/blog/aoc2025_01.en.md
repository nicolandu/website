+++
title = "Advent of Code Optimisations: 2025, Day&nbsp;1"
date = "2025-12-02"
description = "Of replacing brute-force search with arithmetic calculations."

[taxonomies]
tags = ["Advent of Code"]
+++

```rust
pub fn part_two(input: &str) -> Option<i64> {
    let mut t: i64 = 50;
    let mut c: i64 = 0;
    for line in input.lines() {
        let (begin, end) = line.split_at_checked(1)?;
        let d = end.parse::<i64>().ok()?;
        let t_old = t;
        match begin {
            "R" => {
                t += d;
                c += t.div_euclid(100) - t_old.div_euclid(100);
            }
            "L" => {
                t -= d;
                c += (t_old - 1).div_euclid(100) - (t - 1).div_euclid(100);
            }
            _ => (),
        }
        t = t.rem_euclid(100);
    }
    Some(c)
}
```
